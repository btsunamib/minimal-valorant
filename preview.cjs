const http=require('http'),fs=require('fs'),path=require('path');const args=process.argv;const pi=args.indexOf('--port');const port=Number(pi>=0?args[pi+1]:process.env.PORT||4173);const root=path.join(__dirname,'dist');const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml'};http.createServer((req,res)=>{let u;try{u=decodeURIComponent(new URL(req.url,'http://localhost').pathname)}catch{res.writeHead(400);return res.end()}const file=path.resolve(root,'.'+(u==='/'?'/index.html':u));if(!file.startsWith(root+path.sep)){res.writeHead(403);return res.end()}fs.readFile(file,(err,data)=>{if(err){res.writeHead(404);return res.end('Not found')}res.setHeader('Content-Type',mime[path.extname(file)]||'application/octet-stream');res.end(data)})}).listen(port,'0.0.0.0',()=>console.log('Local preview ready on '+port));
